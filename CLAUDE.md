# 黑马记账 — 项目文档

> 本文件是本项目的"总指挥手册"，记录了产品需求、技术规则和开发进度。
> 每次开发前，Claude 必须先阅读本文件并遵守其中的规则。

---

## 一、项目简介

- **项目名称**：黑马记账
- **项目类型**：桌面记账应用（Windows + Mac 双平台）
- **核心功能**：记录每笔花销（人民币）和收入，按 2 级分类归档
- **数据存储**：仅保存在用户本机，不联网、不上传
- **使用人群**：非技术用户，界面全部使用简体中文
- **开发者**：Claude Code（本项目的编程助手），用户负责做决定

---

## 二、产品需求文档

### 2.1 核心功能

1. **记一笔（记账）**：记录每笔花销或收入
   - 金额（人民币，支持小数点后 2 位）
   - 收支类型（支出 / 收入）
   - 2 级分类（先选一级大类，再选二级小类）
   - 日期（默认当天，可改）
   - 备注（可选）
2. **流水查看**：按日期分组显示全部账单，支持编辑和删除
3. **收入记录**：收入分类记录，自动计算每月结余
4. **统计图表**：月度支出趋势、各分类支出占比、收支对比
5. **预算提醒**：设置每月预算，接近或超过时界面提示
6. **数据导出**：一键导出全部账目为 Excel 文件

### 2.2 分类设计（2 级）

#### 支出 · 10 个大类

| 一级大类 | 二级小类 |
|---|---|
| 餐饮 | 早餐 / 午餐 / 晚餐 / 外卖 / 零食饮料 / 水果 / 咖啡茶饮 / 聚餐请客 |
| 交通 | 公交地铁 / 打车 / 加油 / 停车 / 火车高铁 / 飞机 / 共享单车 / 车辆维修保养 |
| 购物 | 服装鞋帽 / 日用品 / 数码电器 / 美妆护肤 / 家居用品 / 宠物用品 / 烟酒 / 书籍文具 |
| 居住 | 房租 / 房贷 / 水电燃气 / 物业费 / 家具家电 / 装修维修 |
| 娱乐 | 电影演出 / 游戏充值 / 健身运动 / 旅游度假 / KTV聚会 / 影音会员 |
| 医疗健康 | 门诊挂号 / 药品 / 体检 / 住院 / 保健品 |
| 教育学习 | 学费 / 培训课程 / 考试报名 / 学习用品 |
| 通讯网络 | 手机话费 / 流量 / 宽带 |
| 人情往来 | 红包礼金 / 份子钱 / 请客送礼 |
| 其他 | 其他支出 |

#### 收入 · 3 个大类

| 一级大类 | 二级小类 |
|---|---|
| 工资收入 | 基本工资 / 奖金 / 补贴 |
| 理财收入 | 利息 / 基金股票收益 |
| 其他收入 | 红包 / 报销 / 兼职 / 其他 |

> 分类数据在数据库初始化时写入，后续如需增删分类，需用户确认后修改。

### 2.3 数据存储约定

- 使用 SQLite 数据库，文件保存在用户数据目录（`userData`），仅本机可访问
- **金额以"分"为单位的整数存储**，避免小数计算误差（如 12.34 元存为 1234）
- 显示时再转换为元

---

## 三、技术决策协议（核心规则，必须严格遵守）

### 3.1 背景

用户**不懂编程技术**，无法提出具体的技术要求，也是初次使用 Claude Code。
因此，任何技术相关的事情，**必须由 Claude 列出方案、向用户解释，由用户决定**。

### 3.2 规则

1. **重大技术决策必须由用户选择**：任何涉及框架、库、架构、数据存储、界面设计风格等对用户有影响的技术决策，Claude 必须列出**至少 2 个可选方案**，用**通俗语言**说明每个方案的优劣势和对用户的影响（如体积、速度、后续维护），由用户选择决定后才执行。
2. **不得擅自决定**：用户未确认之前，Claude 不得替用户决定重大技术方向。
3. **琐碎细节可自行处理**：不影响用户利益的内部实现细节（如某个函数怎么写、某个按钮怎么摆），Claude 可自行决定，但需在回复中简单交代。
4. **决策留档**：每次用户做出的技术决策，必须追加记录到下方"技术决策记录"表（日期、决策内容、原因）。
5. **用户无法决定时**：Claude 给出推荐方案并说明理由，等待用户确认后再执行。

### 3.3 沟通方式

- 始终使用**通俗语言**，避免专业术语；必须使用术语时，当场用大白话解释
- 用"你"称呼用户，解释时多用类比（如"相当于……"）

---

## 四、技术决策记录

| 日期 | 决策项 | 用户选择 | 原因/备注 |
|---|---|---|---|
| 2026-08-16 | 跨平台技术方案 | 方案A：Electron + Vue 3 | 最成熟稳定、资料最多、出问题最好解决 |
| 2026-08-16 | 数据存储方式 | 仅保存在本机（SQLite） | 简单可靠、隐私好；个人记账够用 |
| 2026-08-16 | 功能范围 | 核心记账 + 统计图表 + 预算提醒 + 收入记录 + 数据导出 | 用户全选 |
| 2026-08-16 | 界面组件库 | Element Plus | 成熟、中文文档完善（琐碎细节，Claude 决定） |
| 2026-08-16 | 图表库 | ECharts | 中文出品、文档完善、图表美观（琐碎细节，Claude 决定） |

---

## 五、技术栈与项目结构

### 5.1 技术栈

- **Electron**：桌面应用外壳（让网页技术跑在电脑上）
- **Vue 3 + Vite**：界面框架与构建工具
- **Element Plus**：界面组件库（按钮、弹窗、表单等现成组件）
- **ECharts**：统计图表
- **better-sqlite3**：本机数据库
- **SheetJS (xlsx)**：Excel 导出
- **electron-builder**：打包安装程序

### 5.2 目录结构（electron-vite 标准结构）

```
项目根目录/
├── CLAUDE.md               ← 本文件（项目手册）
├── src/
│   ├── main/               ← 主进程（数据库、文件操作等"后台"工作）
│   │   └── db/             ← 数据库初始化与查询
│   ├── preload/            ← 安全桥（界面与后台之间的"传话筒"）
│   └── renderer/           ← 界面代码（Vue）
│       └── src/
│           ├── views/      ← 页面：记一笔/流水/统计/预算/设置
│           └── components/ ← 复用组件
```

---

## 六、开发进度

- [x] 产品文档与项目规则（本文件）
- [x] 环境检查（Node.js 已就绪）
- [x] 项目初始化（Electron 43 + Vue 3.5 + electron-vite 5）
- [x] 数据库与分类数据（74 个分类：支出 10 大类 52 小类，收入 3 大类 9 小类）
- [x] 记一笔页（金额键盘、收支切换、2 级分类、日期、备注）
- [x] 流水页（按月分组、编辑、删除）
- [x] 统计页（趋势图、分类占比饼图、每日支出柱状图）
- [x] 预算页（月度预算、进度条、超支提醒）
- [x] 设置页（Excel 导出）
- [x] Windows 打包验证（`dist/黑马记账 Setup 1.0.0.exe`，win-unpacked 为免安装版）
- [ ] Mac 打包（需 Mac 电脑，代码相同）

### 开发小记（重要事实，避免重复踩坑）

1. **代码必须用 ESM 写法**：electron-vite v5 对 CJS `require()` 本地模块不再打包（会报 `Cannot find module './db'`）。主进程/preload/数据库模块一律用 `import`/`export` 写法。package.json 已设 `"type": "module"`，`"main"` 指向 `out/main/index.js`（构建产物为 ESM 内容，文件名保持 index.js；preload 产物为 index.mjs，主进程里引用的 preload 路径必须写 `index.mjs`）。
2. **图标脚本**：scripts/gen-icon.cjs（ESM 项目里 .js 会被当 ESM 解析，所以用 .cjs），运行 `npm run gen:icon` 重新生成 build/icon.ico。
3. **本机环境**：这台电脑的终端会话带有 `ELECTRON_RUN_AS_NODE=1` 环境变量（未写入注册表，仅当前会话注入），会导致 Electron 以"纯 Node"模式运行、窗口打不开。运行 `npm run dev` 前先 `unset ELECTRON_RUN_AS_NODE`（打包后的 exe 双击不受影响）。
4. **npm 镜像**：.npmrc 已配置 npmmirror 国内镜像加速（npm 会提示 electron_mirror 键"未知"，属无害警告，忽略即可）。
5. **打包配置**：electron-builder.yml 里 `npmRebuild: false`（本机无 Visual Studio 编译环境；better-sqlite3 是 N-API 通用模块，dev 验证过的二进制直接可用）。打包前需 `export ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/` 走国内镜像下载打包工具。
6. **用户数据目录**：实测打包版与开发版共用 `%APPDATA%\heima-bookkeeping\heima.db`（Electron 读 package.json 的 name 而非 productName）。数据互通无碍；若将来想改目录名需在 main 里 `app.setName()` 并迁移数据。
